node_modules/
.next/
dist/
.env
.env.local
*.tsbuildinfo
.agents
skills-lock.json
.claude
# Real school data — 27 named teachers from a live school. Imported at
# runtime via /timetable/import, never committed and never seeded.
New Time table*.xlsx

# One-time credential handovers written by provisionTimetableSchool.ts
credentials-*.txt
